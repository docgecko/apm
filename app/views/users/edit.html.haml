- @page_title =  "Edit Existing User"

= form_for resource, :url => account_path do |f|
	= f.error_messages
	= render :partial => "form", :object => f
	= f.submit "Update"
 
%br= link_to "My Profile", account_path

